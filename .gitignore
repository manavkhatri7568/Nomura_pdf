# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/
menv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local config / secrets — never commit real Graph credentials
.env
.env.*
*.pem
*.key

# A sample run (inbox, processed cases, SQLite index, logs) is tracked so the
# output is visible on GitHub. Regenerate locally with demo.py / generate_test_emails.py.
# Only the mutable runtime-config override (written by the /config API) is ignored.
data/runtime_config.json

# Next.js
frontend/.next/
frontend/node_modules/
frontend/.env.local
frontend/.env.production

# OS
.DS_Store
Thumbs.db
